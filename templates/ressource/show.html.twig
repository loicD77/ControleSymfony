{% extends 'base.html.twig' %}

{% block title %}Ressources{% endblock %}

{% block body %}
    <h1>Ressources</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ressource.id }}</td>
            </tr>
            <tr>
                <th>Intitulé</th>
                <td>{{ ressource.Intitulé }}</td>
            </tr>
            <tr>
                <th>Présentation</th>
                <td>{{ ressource.Présentation }}</td>
            </tr>
            <tr>
                <th>Support</th>
                <td>{{ ressource.Support ? ressource.Support|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Nature</th>
                <td>{{ ressource.Nature ? ressource.Nature|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Urldoc</th>
                <td>{{ ressource.urldoc }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ressource_index') }}">back to list</a>

    <a href="{{ path('app_ressource_edit', {'id': ressource.id}) }}">edit</a>

    {{ include('ressource/_delete_form.html.twig') }}
{% endblock %}
